<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.photoapp">

    <uses-permission android:name="android.permission.MANAGE_DOCUMENTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.photoapp.CreateAlbumActivity" android:label="PhotoApp" />
        <activity android:name="com.example.photoapp.EditAlbumActivity" android:label="PhotoApp" />
        <activity android:name="com.example.photoapp.OpenAlbumActivity" android:label="PhotoApp" />
        <activity android:name="com.example.photoapp.PhotoDisplayActivity" android:label="PhotoApp" />
        <activity android:name="com.example.photoapp.CreateTagActivity" android:label="PhotoApp" />
    </application>

</manifest>